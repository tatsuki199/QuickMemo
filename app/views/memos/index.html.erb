<div class="new_link" >
  <%= link_to "新規投稿", new_memo_path %>
</div>
<ul class="item-lists">
  <% @memos.each do |memo| %>
    <%= render partial: "memos/memo", locals: {memo: memo} %>
  <% end %>
</ul>